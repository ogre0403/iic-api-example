services:
  api-docs:
    image: nginx:alpine
    container_name: iic-api-docs
    ports:
      - "8080:80"
    volumes:
      - ./web:/usr/share/nginx/web:ro
      - ./swagger:/usr/share/nginx/swagger:ro
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
